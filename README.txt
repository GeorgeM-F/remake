Questo è un tentativo di replicare "pre-assessment" tramite FastAPI & React.

Attualmente incompleto (esiste solo la funzione di signup in backend).

La cartella "backend" contiene il backend (in FastAPI);
la cartella "frontend" contiene il frontend (in React).

Testare la versione attuale del backend:
1) Entra in "backend" ed esegui "source venv/bin/activate".
2) Entra in "backend" ed esegui "uvicorn main:app --reload".
3) Visita l'indirizzo: "http://127.0.0.1:8000/docs".
